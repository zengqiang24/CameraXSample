package com.example.cameraxsample

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class ShowAllCameraActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.all_cameras_layout)
//        val fragment = BasePreviewFragment.createFragment(LEFT_CAMERA_ID)
//        supportFragmentManager.beginTransaction().replace(R.id.gridFrame1, fragment).commit()
//        val fragment2 = BasePreviewFragment.createFragment(RIGHT_CAMERA_ID)
//        supportFragmentManager.beginTransaction().replace(R.id.gridFrame2, fragment2).commit()
//        val fragment3 = BasePreviewFragment.createFragment(FRONT_CAMERA_ID)
//        supportFragmentManager.beginTransaction().replace(R.id.gridFrame3, fragment3).commit()
//        val fragment4 = BasePreviewFragment.createFragment(BACK_CAMERA_ID)
//        supportFragmentManager.beginTransaction().replace(R.id.gridFrame4, fragment4).commit()
    }

}